# Public URL used for canonical metadata, robots and sitemap generation.
# Keep a full absolute URL and omit trailing slash.
NEXT_PUBLIC_SITE_URL=https://example.com

# Enable booking submission debug logs in browser (development only).
# Accepted values: 1, true, yes, on
NEXT_PUBLIC_ENABLE_BOOKING_DEBUG_LOGS=false